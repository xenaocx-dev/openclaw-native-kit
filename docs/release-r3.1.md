# 2026.9.4 r3.1 — 公开分享版

r3.1 基于已评审的 r3，只处理公开分享的包装和许可证；没有新的运行逻辑。

- 自有代码、配置模板及文档采用 MIT，第三方文件继续保留原许可。
- 保留 OpenClaw 2026.9.4 完整第三方 notices；为修改过的 Claude adapter 添加 Apache-2.0 修改声明。
- adapter 只有文件头注释变化，删除该声明后与 r3 字节相同。manifest 接受先前 adapter hash，目标总数仍为 20。
- 新增面向使用者和开发者的分享介绍，安装入口仍使用现有 Agent 任务说明。

升级核对：完整 r3 为 1 ready-replace / 19 installed（仅注释）；r2 为 3 ready-replace / 17 installed（r3 的两处修复加注释）；r1 为 7 ready-replace / 13 installed。安装器、配置和认证不变，已有用户无需为了注释更新运行中的安装。

原始 r3 tag 与安装包保留，不替换历史文件。新用户请使用 v2026.9.4-r3.1。它继续标记为预发布版：已通过定向测试和 r3 Fable 评审，但 r3 运行修复尚未部署到维护者生产环境；真实后端取消和完整 Telegram E2E 未重新验证。

生产实例保持原状。历史评审、部署快照均按各自日期理解，不代表当前安装已自动升级。
